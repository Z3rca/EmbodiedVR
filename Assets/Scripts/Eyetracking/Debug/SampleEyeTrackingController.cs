using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SampleEyeTrackingController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
         if (Input.GetKeyDown(KeyCode.A))
        {
            EyetrackingManager.Instance.StartCalibration();
        }
        
        if (Input.GetKeyDown(KeyCode.R))
        {
            EyetrackingManager.Instance.StartRecording();
        }
        if (Input.GetKeyDown(KeyCode.H))
        {
            EyetrackingManager.Instance.StopRecording();
        }
        
        if (Input.GetKeyDown(KeyCode.W))
        {
            EyetrackingManager.Instance.SaveEyetrackingData();
        } 
         */
    }
}
